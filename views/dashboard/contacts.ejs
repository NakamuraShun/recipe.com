<%- include("../partials/_header-dashboard") %>


<!-- ================ header: #Header ================  -->
<header id="Header" class="bg-secandary">
	<div class="container py-4 mb-4">
		<h1>
			<i class="fas fa-envelope" style="color:#007bff;"></i> メール管理</h1>
	</div><!-- /.container -->
</header><!-- /#Header -->

<!-- flashes  -->
<div class="container">
	<% if(flashMsg.success){ %>
	<div class="alert alert-success" role="alert">
		<%= flashMsg.success %>
		<button type="button" class="ml-2 mb-1 close" data-dismiss="alert" aria-label="閉じる">
			<span aria-hidden="true">&times;</span>
		</button>
	</div>
	<% }else if(flashMsg.error){ %>
	<div class="alert alert-danger" role="alert">
		<%= flashMsg.error %>
		<button type="button" class="ml-2 mb-1 close" data-dismiss="alert" aria-label="閉じる">
			<span aria-hidden="true">&times;</span>
		</button>
	</div>
	<% } %>
</div>


<!-- ================ section:#Dashboard-wrap ================  -->
<section id="Dashboard-wrap" class="mb-5">
	<div class="container">
		<div class="row">

			<!-- **** #Dashboard-list ****  -->
			<div class="col" id="Dashboard-list">
				<div class="card">
					<div class="card-header">
						<h4>メール情報一覧</h4>
					</div>
					<div class="card-body table-responsive p-0">
						<table class="table text-nowrap table-striped">
							<thead class="bg-primary text-light text-center">
								<tr>
									<th>受信日時</th>
									<th>メールID</th>
									<th>件名</th>
									<th></th>
									<th></th>
								</tr>
							</thead>
							<tbody class="text-center">
								<% contacts.forEach( contact =>{ %>
								<tr>
									<td><%= contact.createdAt %></td>
									<td><%= contact._id %></td>
									<td><%= contact.title %></td>
									<td>
										<a href="<%= `/dashboard/contacts/${contact._id}` %>"
											class="btn btn-outline-secondary">詳細</a>
									</td>
									<td>
										<a href="<%= `/dashboard/contacts/${contact._id}/delete?_method=DELETE` %>"
											class="btn btn-outline-danger DeleteBtn">削除</a>
									</td>
								</tr>
								<% }); %>
							</tbody>
						</table>
					</div>
					<div class="card-footer">
						<div>
							<a href="/dashboard">＜ ダッシュボードトップに戻る</a>
						</div>
					</div>
				</div>
			</div><!-- /.col #Dashboard-list -->

		</div><!-- /.row -->
	</div><!-- /.container -->
</section><!-- /#Dashboard-wrap (#Dashboard-list / #Sidenav-btn)  -->

<%- include("../partials/_footer-dashboard") %>